## Memories

- `jupyter-book build .` was run from the repository root using the project’s virtual environment.
- The build progressed through parsing markdown and bibtex entries, and then failed when executing notebooks via `myst-nb`/`nbclient`.


